using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StarController : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(GetComponent<GameObject>(), 0.1f);
    }

    // Update is called once per frame
    void Update () {
		
	}
    

}
